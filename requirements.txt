numpy==1.26.4
pandas

# NOTE: torch is installed separately from the CUDA wheel index (cu121) BEFORE
# running this file. Do NOT list torch here, or pip pulls the CPU build from
# PyPI and overwrites the CUDA build (local version +cu121 sorts BELOW the
# plain PyPI torch, so pip "upgrades" it to the CPU build).
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

einops==0.8.1
huggingface_hub==0.33.1
matplotlib==3.9.3
pandas==2.2.2
tqdm==4.67.1
safetensors==0.6.2
yfinance>=0.2.40

# Optional: only needed when using real FinBERT text embeddings for early fusion.
# Pipeline still runs without it (falls back to offline hash embeddings).
transformers>=4.30.0
